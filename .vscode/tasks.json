{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Release to Clones (dev → main)",
            "type": "shell",
            "command": "git push origin dev:main; echo 'Released! Clones will update within 15 minutes.'",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "presentation": {
                "reveal": "always",
                "panel": "shared",
                "focus": true
            },
            "problemMatcher": []
        }
    ]
}
